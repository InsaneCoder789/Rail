{
  "name": "rail",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Rail — offline-first payment orchestration (PostgreSQL, HMAC/HSM hooks, sync API)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "migrate": "npm run build && node dist/persistence/runMigrations.js",
    "demo": "npm run build && node dist/demo/runPayment.js",
    "server": "npm run build && node dist/server/server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^22.13.10",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.3"
  }
}
